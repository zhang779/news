<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/wang_yi_title_bg</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--主页底部按钮-->
    <style name="home_bottom_style">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_gravity">center_vertical</item>
        <item name="android:layout_weight">1</item>
        <item name="android:button">@null</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="android:textSize">10sp</item>
        <item name="android:background">@color/transparent</item>
        <item name="android:textColor">@drawable/rb_bottom_select</item>
    </style>

    <!--下拉刷新-->
    <style name="pull_style">
        <!--头部水滴颜色-->
        <item name="waterDropColor">@color/wang_yi_title_bg</item>
        <!--尾部指示箭头颜色-->
        <item name="indicatorArrowColor">@color/wang_yi_title_bg</item>
        <!--loading 小控件开始颜色-->
        <item name="loadStartColor">#FFFFFFFF</item>
        <!--loading 小控件结束颜色-->
        <item name="loadEndColor">@color/wang_yi_title_bg</item>
        <!--尾部文字颜色-->
        <item name="android:textColor">@color/wang_yi_title_bg</item>
        <!--头部箭头颜色-->
        <item name="refreshArrowColor">@color/white</item>
    </style>

    <style name="ColorTranslucentTheme" parent="AppTheme"></style>
    <!--正在加载时的进度dialog-->
    <style name="dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowIsFloating">true</item>
        <!-- 设置未浮动窗口 -->
        <item name="android:windowFrame">@null</item>
        <!-- 设置无边框 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 设置无标题 -->
        <item name="android:windowBackground">@color/transparent</item>
        <!-- 设置完全透明 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 设置屏幕变暗 -->
    </style>
</resources>
